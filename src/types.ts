export type AddressType = {
    id: number
    value: string
    postalCode: string
}

export type ProductType = {
    id: number
    title: string
    quantity: number
}

export type UserType = {
    _id: string
    name: string,
    age: number
}